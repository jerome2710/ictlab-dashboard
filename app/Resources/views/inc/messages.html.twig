{% for type, messages in app.session.flashBag.all() %}
	<div class="container">
		<div class="notification notification--bar notification--{{ type }}">
			{% for message in messages %}
				<p class="notification__message">{{ message|raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}